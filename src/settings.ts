/** Must match the "platform" value used in config.json and config.schema.json. */
export const PLATFORM_NAME = 'AlfenCharger';

/** Must match the "name" field in package.json. */
export const PLUGIN_NAME = 'homebridge-alfen-charger';
